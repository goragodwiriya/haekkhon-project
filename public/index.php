<?php // public/index.php

require_once '../haekkhon.php';

// =================== HOME PAGE ===================
route('/', function () {
    require __DIR__.'/partials/header.php';
    ?>
    <main>
        <section class="hero">
            <div class="container">
                <h1>Haekkhon PHP Framework</h1>
                <p>เบา บาง และเข้าใจง่ายที่สุด สร้างขึ้นมาเพื่อให้คุณเข้าใจหลักการทำงานของ Framework แบบพื้นฐานที่สุด ไม่มีอะไรซับซ้อน ไม่มี Magic ที่ซ่อนอยู่</p>
                <a href="docs" class="btn">เริ่มต้นใช้งาน</a>
            </div>
        </section>
        <section class="section">
            <div class="container">
                <h2>ทำไมต้องใช้?</h2>
                <div class="features-grid">
                    <div class="feature-card">
                        <h3>🚀 เร็วและเบา</h3>
                        <p>ไม่มีสิ่งที่ไม่จำเป็นมากวนใจ โหลดเร็ว ทำงานได้บนเซิร์ฟเวอร์สเปคต่ำ</p>
                    </div>
                    <div class="feature-card">
                        <h3>🧠 เข้าใจง่าย</h3>
                        <p>อ่านโค้ดได้ทันที ไม่ต้องเรียนรู้ Design Pattern ซับซ้อนก่อน</p>
                    </div>
                    <div class="feature-card">
                        <h3>🛠️ ปรับแต่งได้เต็มที่</h3>
                        <p>อยากเพิ่มฟีเจอร์อะไร? แค่เปิดไฟล์แล้วเขียนโค้ดเข้าไปตรงๆ</p>
                    </div>
                </div>
            </div>
        </section>
    </main>
    <?php
require __DIR__.'/partials/footer.php';
});

// =================== DOCS PAGE ===================
route('/docs', function () {
    require __DIR__.'/partials/header.php';
    ?>
    <main class="section">
        <div class="container docs-content">
            <h2>เอกสารการใช้งาน</h2>
            <h3>การติดตั้ง</h3>
            <p>แค่ดาวน์โหลดโค้ดและวางไว้บน Web Server ที่รองรับ PHP และ URL Rewriting</p>

            <h3>การกำหนดเส้นทาง (Routing)</h3>
            <p>เปิดไฟล์ <code>public/index.php</code> แล้วใช้ฟังก์ชัน <code>route()</code> เพื่อสร้างเส้นทางใหม่</p>
            <pre><code>route('/my-page', function() {
    echo "นี่คือหน้าของฉัน!";
});</code></pre>

            <h3>การเริ่มต้น</h3>
            <p>อย่าลืมเรียกฟังก์ชัน <code>dispatch()</code> ที่ท้ายไฟล์เพื่อเริ่มการทำงานของ Router</p>
            <pre><code>dispatch();</code></pre>
        </div>
    </main>
    <?php
require __DIR__.'/partials/footer.php';
});

// =================== FEATURES PAGE ===================
route('/features', function () {
    require __DIR__.'/partials/header.php';
    ?>
    <main class="section">
        <div class="container">
            <h2>ความสามารถทั้งหมด</h2>
            <div class="features-grid">
                <div class="feature-card"><h3>✨ Routing พื้นฐาน</h3><p>จับคู่ URL กับฟังก์ชันได้อย่างง่ายดาย</p></div>
                <div class="feature-card"><h3>🔒 Single Entry Point</h3><p>ความปลอดภัยขั้นพื้นฐานผ่านการเข้าถึงที่จุดเดียว</p></div>
                <div class="feature-card"><h3>📁 Subdirectory Support</h3><p>ติดตั้งในโฟลเดอร์ย่อยได้โดยไม่ต้องกังวลเรื่อง URL</p></div>
                <div class="feature-card"><h3>🎨 ไม่ผูกมัดกับ UI</h3><p>คุณเป็นคนกำหนดว่าจะใช้ CSS หรือ Template Engine แบบไหน</p></div>
                <div class="feature-card"><h3>🧩 ขยายได้</h3><p>พื้นฐานที่พร้อมจะพัฒนาต่อเป็น Framework ของคุณเอง</p></div>
                <div class="feature-card"><h3>❤️ สร้างด้วยใจ</h3><p>สร้างขึ้นมาเพื่อความสนุกและการเรียนรู้</p></div>
            </div>
        </div>
    </main>
    <?php
require __DIR__.'/partials/footer.php';
});

// =================== DEMO PAGE ===================
route('/demo', function () {
    require __DIR__.'/partials/header.php';
    ?>
    <main class="section">
        <div class="container docs-content">
            <h2>ทดลองใช้งานจริง</h2>
            <p>หน้านี้แสดงข้อมูลบางอย่างจาก Server ผ่าน PHP ซึ่งเป็นการพิสูจน์ว่า Framework ของเราทำงานได้จริง!</p>

            <h3>ข้อมูลเซิร์ฟเวอร์</h3>
            <ul>
                <li><strong>เวอร์ชัน PHP:</strong> <?php echo phpversion(); ?></li>
                <li><strong>IP Address ของคุณ:</strong> <?php echo $_SERVER['REMOTE_ADDR']; ?></li>
                <li><strong>User Agent:</strong> <?php echo $_SERVER['HTTP_USER_AGENT']; ?></li>
            </ul>

            <h3>วันที่และเวลาปัจจุบันบน Server</h3>
            <p><?php echo date('l, j F Y, H:i:s'); ?></p>
        </div>
    </main>
    <?php
require __DIR__.'/partials/footer.php';
});

// เริ่มต้นการทำงานของ Router
dispatch();